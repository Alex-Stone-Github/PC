#include "token.h"

Token createToken(TOK_TYPE type, int value, INOUT name) {
    if (type == INSTRUCTION)
        int a;
    if (type == ENDLINE) 
        int b;
    if (type == INOUTARG) 
        int b;
    if (type == NUMBER) 
        int b;
}
